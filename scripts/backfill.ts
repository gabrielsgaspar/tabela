// Backfill script — Phase 6 placeholder.
// Runs run-once for a range of historical dates and persists the results.
// Full implementation deferred until the Supabase schema (Step 5) and the
// Trigger.dev task (Step 6) are in place.
//
// Usage (once implemented):
//   pnpm backfill -- --from 2026-04-27 --to 2026-05-03

console.log(
  "backfill: not yet implemented — comes online in Step 6 after the Supabase schema is set up.",
);
process.exit(0);
