# RECOVERY_PLAN.md

**Generated:** 2026-05-04  
**Basis:** VERIFICATION_REPORT.md findings + full triage of untracked/modified files.

---

## Part 1 — File triage

### Modified files (2)

| File | Assessment | Action |
|------|------------|--------|
| `.gitignore` | ✅ Keep as-is. The original commit was the F1 repo's Python-centric `.gitignore`; the current version is correct for a Node.js/Next.js project. Ignore rules for `node_modules/`, `.next/`, `.env.local`, `output/`, `.trigger/`, `supabase/.temp/` are all right. | Commit |
| `README.md` | ❌ Keep with edits. The content was accidentally replaced with documentation about `claude_design/` — it now opens with `# claude_design/`. The main README should be the project pitch (CLAUDE.md describes it as "full pitch"). The `claude_design/` description is useful but belongs in `claude_design/README.md`. | Fix before committing: write a proper project README, move the design-token reference block to `claude_design/README.md` |

### Untracked files — docs and config (keep as-is)

| File | Assessment | Commit? |
|------|------------|---------|
| `AUDIT.md` | Thorough Phase 6 pre-ship review. Accurate, valuable, no half-written sections. | ✅ Yes |
| `CLAUDE.md` | Project instructions. Complete and authoritative. | ✅ Yes |
| `DATA.md` | API endpoints, schemas, storage spec. Complete. | ✅ Yes |
| `DECISIONS.md` | Architectural decisions log through 2026-05-04. Complete. | ✅ Yes |
| `DEV.md` | Developer workflow (Supabase migrations, running the pipeline locally). References scripts that don't exist yet (`run-once`, `backfill`) but will be built in Step 4 — accurate once that step is done. | ✅ Yes |
| `PLAN.md` | Phase 5 audio plan. Well-written spec for when we get there. No half-written code. | ✅ Yes |
| `ROADMAP.md` | Phase roadmap with Phase 1 marked `← CURRENT`. Accurate. | ✅ Yes |
| `VERIFICATION_REPORT.md` | This audit's output. | ✅ Yes |
| `VOICE.md` | Editorial voice spec. Complete, used by prompts. | ✅ Yes |
| `env.example` | Template for `.env.local`. Missing `NEXT_PUBLIC_APP_URL` and `NEXT_PUBLIC_APP_NAME` (added in Step 1). Otherwise complete. | ✅ Yes (updated in Step 1) |

### Untracked files — Next.js / tooling config (keep as-is)

| File | Assessment | Commit? |
|------|------------|---------|
| `next-env.d.ts` | Auto-generated by Next.js. Conventionally committed so TypeScript can see route types. No secrets. | ✅ Yes |
| `next.config.ts` | Minimal and correct. Sets `images.remotePatterns` for `crests.football-data.org`. | ✅ Yes |
| `package.json` | Contains the broken `lint` script and is missing `@anthropic-ai/sdk`. Fixed in Step 1 before this commit lands. | ✅ Yes (updated in Step 1) |
| `pnpm-lock.yaml` | Lock file matching current `node_modules/`. Updated in Step 1 after `@anthropic-ai/sdk` install. | ✅ Yes (updated in Step 1) |
| `postcss.config.mjs` | PostCSS config for Tailwind v4 (`@tailwindcss/postcss`). Correct. | ✅ Yes |
| `trigger.config.ts` | Trigger.dev config. Points to `./src/trigger` (dir not yet built). Config itself is correct. | ✅ Yes |
| `tsconfig.json` | TypeScript config. Strict mode on, `@/*` path alias to `src/`. Correct. | ✅ Yes |

### Untracked files — source code (keep as-is)

| File | Assessment | Commit? |
|------|------------|---------|
| `src/app/globals.css` | Design tokens (`@theme`), base styles, typography classes. Thorough, matches `claude_design/`. | ✅ Yes |
| `src/app/layout.tsx` | Root layout with Newsreader / DM Sans / JetBrains Mono fonts, correct metadata. | ✅ Yes |
| `src/app/page.tsx` | Placeholder (`<main>Tabela</main>`). Minimal but intentional — website comes in Phase 4. | ✅ Yes |
| `src/audio/types.ts` | `SynthesisInput`, `SynthesisResult`, `EditorialRef`. Complete, no implementation yet. | ✅ Yes |
| `src/editorial/prompts.ts` | Four prompt builder functions for all editorial formats. Prompt caching wired. Well-crafted. | ✅ Yes |
| `src/editorial/types.ts` | Input/output types for the editorial layer. Complete. | ✅ Yes |
| `src/lib/football-types.ts` | Football-Data.org v4 response types. Matches DATA.md sample shapes exactly. | ✅ Yes |

### Untracked files — Supabase (keep as-is)

| File | Assessment | Commit? |
|------|------------|---------|
| `supabase/config.toml` | Local Supabase CLI config. Standard defaults. No secrets. | ✅ Yes |
| `supabase/.temp/cli-latest` | CLI version file, generated by `supabase init`. Ignored by `.gitignore` (`supabase/.temp/`). | ❌ Already gitignored — will not appear |

### Untracked files — claude_design (keep as-is, never modify)

| File | Assessment | Commit? |
|------|------------|---------|
| `claude_design/Tabela.html` | Visual spec for the home page. Reference-only. | ✅ Yes |
| `claude_design/Tabela_-_Team__League___Archive.html` | Visual spec for team/league/archive pages. Reference-only. | ✅ Yes |

### Do not commit

| Path | Reason |
|------|--------|
| `dist/tsconfig.tsbuildinfo` | TypeScript incremental build cache. Covered by `.gitignore` (`*.tsbuildinfo` + `dist/`). |
| `.env.local` | Secrets. Gitignored. Never commit. |
| `.next/` | Build output. Gitignored. |

---

## Part 2 — README fix

The current `README.md` opens with `# claude_design/` — it was accidentally replaced with the design reference notes. Two changes needed before the initial commit:

1. **`README.md`** → rewrite as the project pitch (see content proposed below).
2. **`claude_design/README.md`** → new file containing the current `README.md` content (design tokens, file descriptions, "do not edit" warning). The `claude_design/` spec is useful reference but belongs inside that folder, not at the project root.

**Proposed `README.md` content (for approval):**

```markdown
# Tabela

A daily editorial briefing on the top five European football leagues.

Every morning a scheduled task fetches the previous day's matches, updates 
season statistics, asks Claude to write a short editorial in a warm, 
allusive voice, generates an audio version, and publishes the result to 
a Next.js site.

## Leagues

Premier League · La Liga · Bundesliga · Serie A · Ligue 1

## Stack

Next.js 16 · Trigger.dev v3 · Supabase (Postgres + Storage) · Anthropic Claude · ElevenLabs

## Docs

| File | Purpose |
|------|---------|
| `CLAUDE.md` | Project instructions and conventions — start here |
| `ROADMAP.md` | Phase milestones and current position |
| `DECISIONS.md` | Architectural decisions, append-only log |
| `DATA.md` | API endpoints, data shapes, storage schema |
| `VOICE.md` | Editorial voice spec — required reading before touching prompts |
| `DEV.md` | Developer workflow (migrations, running the pipeline) |
| `AUDIT.md` | Phase 6 pre-ship review |

## Running locally

```bash
pnpm install
pnpm dev          # Next.js dev server
pnpm trigger:dev  # Trigger.dev local task runner
pnpm typecheck
pnpm lint
```

Copy `env.example` to `.env.local` and fill in the values before running anything.
```

---

## Part 3 — Commit ordering note

Your instructions list "Fix blockers" as Step 1 and "Commit scaffolding" as Step 2. Since both operate on the same set of uncommitted files, the cleanest git history is to apply the Step 1 changes to the working directory first, then commit all of it together. This gives:

```
[initial commit — 6c0c06a] ← stays as-is (the F1 fork)
         ↓
[recovery baseline] ← Step 1 fixes + all scaffolding in one commit
         ↓
[Step 4: football client + run-once]
         ↓
[Step 5: Supabase schema + client]
         ...
```

The alternative (two separate commits: scaffolding then fixes) would produce an intermediate commit where `pnpm lint` is still broken and `@anthropic-ai/sdk` is missing — which is exactly the state we're recovering from. One clean baseline is preferable.

**If you prefer two separate commits, say so and I'll split them.** Otherwise I'll proceed with the bundled approach.

---

## Part 4 — Ordered recovery steps

### Step 0 (now) — This document. ← WAITING FOR APPROVAL

### Step 1+2 — Recovery baseline commit

Apply to working directory (all uncommitted files):
- `pnpm add @anthropic-ai/sdk` → updates `package.json` and `pnpm-lock.yaml`
- Fix `lint` script in `package.json`: `"eslint src/ --ext .ts,.tsx"` → removes Next.js 16 `next lint` error
- Add `NEXT_PUBLIC_APP_URL` and `NEXT_PUBLIC_APP_NAME` to `env.example`
- Fix `README.md` (project pitch) and add `claude_design/README.md` (design token reference)
- Run `pnpm typecheck`, `pnpm lint`, `pnpm build` — all must pass
- Commit everything with message: `chore: recovery baseline — scaffolding, docs, types, prompts`
- Push to origin

**Done signal:** `pnpm typecheck`, `pnpm lint`, `pnpm build` all exit 0. All files committed and pushed.

### Step 3 — Env var audit and fill

- Grep `src/` and `scripts/` for all `process.env.*` references
- Produce exhaustive list with instructions on where to obtain each value
- Stop until you confirm `.env.local` is complete

### Step 4 — Football client + run-once

- Build `src/football/client.ts` (getMatches, getStandings, getScorers)
- Build `scripts/run-once.ts` and stub `scripts/backfill.ts`
- Run against a known-match date, show output
- Append free-tier findings to `DECISIONS.md`
- Commit + push

**Done signal:** `pnpm run-once` produces `output/YYYY-MM-DD.json` with match data for all five leagues (or a clear empty-league log for leagues with no matches that day).

### Step 5 — Supabase schema + client

- Create `supabase/migrations/0001_initial_schema.sql`
- Push migration, verify tables exist
- Run `pnpm supabase:gen-types`
- Build `src/lib/supabase.ts` (server client + browser client)
- Commit + push

**Done signal:** `SELECT table_name FROM information_schema.tables WHERE table_schema='public'` returns `match_days`, `season_stats`, `editorials`, `teams_followed`.

### Step 6 — Daily task + backfill

- Build `src/trigger/daily-report.ts`
- Implement `scripts/backfill.ts` properly
- Run backfill for the last 7 days that had matches
- Show row counts and a sample editorial body
- Commit + push

**Done signal:** `editorials` table has rows with non-null `body` values. At least one body contains a reference grounded in `season_stats` data (e.g. a top-scorer reference).

### Step 7 — Verification re-run

- Run the full 8-layer verification against the new state
- Produce `VERIFICATION_REPORT_2.md`
- Compare against `VERIFICATION_REPORT.md`

---

**Waiting for approval to proceed to Step 1+2.**
